<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.dainv.mymarket">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission
        android:name="android.permission.LOCATION_HARDWARE"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:name=".MyMarketApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/SplashThem">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/LoginThem"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity
            android:name=".ui.register.RegisterActivity"
            android:theme="@style/RegisterStyle"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity
            android:name=".ui.StartActivity"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity
            android:name=".ui.main.MainActivity"
            android:theme="@style/MainActivityTheme" />
        <activity
            android:name=".ui.additem.AddItemActivity"
            android:theme="@style/ActivityAddItem"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.itemdetail.ItemDetailActivity"
            android:exported="true"
            android:launchMode="singleTask" />
        <activity
            android:launchMode="singleTask"
            android:name=".ui.items.ListItemActivity"
            android:theme="@style/ActivityItemsTheme"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name=".service.UploadService"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <provider
            android:name=".searchable.MySuggestionProvider"
            android:authorities="com.example.dainv.mymarket.MySuggestionProvider" />

        <activity android:name=".ui.my.stands.MyStandsActivity" />
        <activity
            android:name=".ui.create.stand.CreateStandActivity"
            android:theme="@style/ActivityAddItem"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.stand.detail.StandDetailActivity"
            android:theme="@style/ActivityStandDetailTheme" />
        <activity android:name=".ui.my.items.MyItemsActivity" />
        <activity android:name=".ui.marked.ItemsMarkedActivity" />
        <activity
            android:name=".ui.notification.SettingNotificationActivity"
            android:theme="@style/SettingNotificationTheme" />
        <activity android:name=".ui.transaction.TransactionActivity"></activity>
        <activity
            android:name="com.example.dainv.mymarket.ui.map.MapActivity"
            android:theme="@style/MapActivityTheme"
            />
        <activity
            android:name=".ui.admin.MainAdminActivity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".ui.addAddress.AddAddressActivity"
            android:theme="@style/LoginThem">

        </activity>
        <activity android:name=".ui.stand_followed.StandFollowedActivity">
        </activity>
        <activity android:name=".ui.ImagePreviewActivity">

        </activity>
        <service android:name=".service.FireBaseService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
     Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_app" />
        <!--
             Set color used with incoming notification messages. This is used when no color is set for the incoming
             notification message. See README(https://goo.gl/6BKBk7) for more.
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />


    </application>

</manifest>